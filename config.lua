cfg = {
  minRadius = 2.5, -- Distance you have to away from a player to grab them
  throwKey = "E", -- Key pressed to throw a person

  forceOffsetX = 35.0, 
  forceOffsetY = 35.0,
  forceOffsetY = 10.0,
}